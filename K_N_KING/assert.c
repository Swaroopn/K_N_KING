#include <stdio.h>

#define NDEBUG  	/* to ignore assert.h */
#include <assert.h>
#include <math.h>

int main(main)
{
    int a[10], i = 10;

    //assert (i >= 0 && i < 10);
    assert(a[i] = 0);
    printf("Success\n");

    printf("%d", pow(2,3));
    return 0;
}
